<div class="green-bg h-100">
  <div class="container pt-5 pl-3">
    <div class="card-white">
      <h1 class="pt-2">Your booking with <%= @pigeon.name %></h1>
      <p><%= @booking.start_date.strftime("%d %b, %Y") %>  -  <%= @booking.end_date.strftime("%d %b, %Y") %></p>
      <img src="https://media2.giphy.com/media/GyKCVW91R83Ek/giphy.gif" class="img-resize">
      <p class="pt-3">Owner: <%= @pigeon.user.username %></p>
      <div class="w-100 d-flex justify-content-between">
        <h2>€<%= @booking.total_price %></h2>
        <%= link_to "Back to #{@pigeon.name}", pigeon_path(@pigeon.id), class: "btn btn-flat" %>
      </div>
    </div>
  </div>
</div>
